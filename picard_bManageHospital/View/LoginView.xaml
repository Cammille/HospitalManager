<Window x:Class="picard_bManageHospital.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="loginView" Height="500" Width="500">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="TextBox.Padding" Value="5"/>
            <Setter Property="TextBox.Width" Value="130"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Width" Value="130"/>
        </Style>
    </Window.Resources>

     <Grid Width="150" Height="200">
        <StackPanel>
            <Label Content="ManageHospital" HorizontalAlignment="Center"/>
            <Label Content="Identifiant" />
            <TextBox x:Name="loginBox"  />
            <Label Content="Password" />
            <PasswordBox x:Name="passwordBox"/>
            <Button Content="Valider" Width="70" Margin="20" Command="{Binding LoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>
